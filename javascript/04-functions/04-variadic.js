/* create a variadic function called sum here and return the results generated by the following commented line inside of it */
// args.reduce((acc, curr) => acc + curr, 0);

function main() {
  if (typeof sum !== "function") {
    console.log("You need to create the sum function");
    return;
  }

  const resultsOne = sum(1, 2, 3);
  const resultsTwo = sum(1);
  const resultsThree = sum(2, 4, 6, 8);
  const resultsFour = sum();

  if (
    resultsOne !== 6 &&
    resultsTwo !== 1 &&
    resultsThree !== 20 &&
    resultsFour !== 0
  ) {
    console.log("Oh no something is not right");
  } else {
    console.log("Great job on the variadic function");
  }
}

main();
